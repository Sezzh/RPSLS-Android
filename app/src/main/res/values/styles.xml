<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay"
           parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>
    <style name="Rules">
        <item name="android:layout_marginTop">
            @dimen/activity_vertical_margin
        </item>
        <item name="android:layout_marginBottom">
            @dimen/activity_vertical_margin
        </item>
    </style>
    <style name="Rules.Title">
        <item name="android:textSize">@dimen/rules_title</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/colorPrimaryDarkText</item>
    </style>
    <style name="PaddingInside">
        <item name="android:padding">@dimen/fab_margin</item>
    </style>
    <style name="RulesBody">
        <item name="android:textSize">@dimen/rules_body</item>
        <item name="android:layout_marginTop">
            @dimen/rules_body_margin
        </item>
        <item name="android:layout_marginBottom">
            @dimen/rules_body_margin
        </item>
    </style>
</resources>
